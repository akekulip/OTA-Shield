# Controller Python dependencies.
#
# NOTE: bfrt_grpc and p4runtime_sh ship with BF-SDE 9.13.2; they are not
# installed from PyPI. Source $SDE/set_env.sh before running the controller
# so the in-SDE Python packages resolve correctly.

# Runtime
grpcio>=1.51.0
grpcio-tools>=1.51.0
protobuf>=4.21.0

# Config & data handling
python-dateutil>=2.8.2

# M6 (IJCIP revision) — signed-manifest + hot-reload RAT pipeline.
# pynacl provides ed25519; inotify_simple provides kernel watches so
# the controller reloads atomically when rat.json or rat.json.sig is
# rewritten on disk. Both are pure-Python wrappers, no compiler needed.
pynacl>=1.5.0
inotify_simple>=1.3.5

# Dev/test
pytest>=7.4.0
